interface:
  display_name: "Mc Creator"
  short_description: "Repo-specific patterns and workflows for mc-creator"
  default_prompt: "Use the mc-creator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true